version: '3'
services:
  redis-master:
    build: sentinel
    ports:
      - "6379:6379"
    environment:
      - REDIS_ROLE=master
  redis-slave:
    build: sentinel
    ports:
      - "6380:6379"
    environment:
      - REDIS_ROLE=slave
  sentinel:
    build: sentinel
    